#include <stdio.h>
int main()
{
 int a,b,temp;
 a=1,b=2;
 printf("before swapping numbers are %d and %d. \n",a,b);
 temp = a;
 a=b;
 b=temp;
  printf("after swapping numbers are %d and %d. \n",a,b);
    return 0;
}
