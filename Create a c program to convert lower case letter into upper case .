// Online C compiler to run C program online
#include <stdio.h>
void main()
{
  char ch ;
  printf("enter any lower case value =");
  scanf("%C",&ch);
  if(ch>=97 && ch<=122)
  {
      ch=ch-32;
      printf("upper case = %c",ch);
  }
}
