// Online C compiler to run C program online
#include <stdio.h> 
int reverse(int num)
{
int r = 0;
while(num>0)
  {
    r= r*10+num%10;
    num=num/10;
  }
return(r);
}
void main()
{
    int a,b;
    printf("enter number =");
    scanf("%d",&a);
    b=reverse(a);
    printf("reverse = %d",b);

}
