#include <stdio.h>
int main() 
{
   int d1,d2,d3,rev,num,ori;
   printf("enter 3 digit number =");
   scanf("%d",&num);
   ori=num;
   d1=num%10;
   num=num/10;
    d2=num%10;
   num=num/10;
    d3=num%10;
    num=num/10;
  rev = d1*100+d2*10+d3;
  printf("reverse of digit %d is %d",ori,rev);
}
